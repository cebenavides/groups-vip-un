<section id="main" >
  <div class="inner">
    <header class="major special">
      <h1>Editing Page</h1>
      <p>Insert the changes</p>
    </header>

	<%= render 'form', page: @page %>
	<%= link_to 'Back', pages_path %>	    
  </div>
</section>

<%= tinymce_assets %>
<%= tinymce %>