<p id="notice"><%= notice %></p>

<section id="main" >
  <div class="inner">
  	<h1><%= @page.title %></h1>
  	<hr>
	<%= @page.content.html_safe %>
	<%= link_to 'Back', group_path(@group), :class => 'button' %>
  </div>
</section>