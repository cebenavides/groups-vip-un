<section id="main" class="container">
	<header>
		<h2><%= @page.title %></h2>
	</header>
	<div class="box">
		<div class="pagecontent">
			<%= @page.content.html_safe %>
		</div>
		<br>
	    <%= link_to 'Back', group_path(@group), :class => 'button small' %>
	</div>
</section>